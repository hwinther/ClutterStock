.DS_Store
.env
node_modules/

# React Router
.react-router/
build/

# Coverage report files
coverage/

# stryker temp files
.stryker-tmp
reports/

# Playwright
playwright-report/
test-results/
playwright/.auth/

# Allure (history travels via GHCR image, report is a CI artifact)
allure-results/
allure-report/
allure-history.jsonl

# Monocart (Playwright browser-side coverage)
monocart-report/
monocart-coverage/

# Artillery load test outputs
load-report.json
load-report.html
# Artillery's TS processor bundler stages CJS output here mid-run; usually
# cleaned up but ignore proactively in case a crash leaves it behind.
load/scenarios/dist/

.env.*
!.env.*.sample
