{
  "//": "Local CommonJS scope for Artillery's processor output. The frontend root package.json declares \"type\": \"module\", but Artillery's built-in TypeScript processor bundles to CJS — without this override, the bundled .js file is treated as ESM and fails to load with `module is not defined`.",
  "type": "commonjs"
}
