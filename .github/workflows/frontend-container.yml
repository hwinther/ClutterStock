name: "Publish frontend container to GHCR"

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"
    paths:
      - "frontend/**"
  # pull_request:
  #   branches:
  #     - main
  #   paths:
  #     - "frontend/**"
  #     - ".github/workflows/frontend-container.yml"

permissions:
  contents: read
  packages: write

concurrency:
  group: "${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}"
  cancel-in-progress: true

jobs:
  frontend_container:
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v'))
    uses: hwinther/reusable-workflows/.github/workflows/docker-container.yml@feat/backend-frontend-container-workflows
    with:
      container_registry: ghcr.io
      container_image_name_postfix: frontend
      build_context: frontend
      dockerfile: Dockerfile
